name: Build Android APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          # هنا تم التحديث للإصدار 21 لحل المشكلة فوراً
          java-version: '21'

      - name: Install Dependencies
        run: |
          npm init -y
          npm install @capacitor/core@latest @capacitor/cli@latest @capacitor/android@latest
          npm install @capacitor/app@latest @capacitor/filesystem@latest @capacitor/share@latest @capacitor/camera@latest

      - name: Prepare Web Assets
        run: |
          mkdir -p www
          cp index.html www/
          cp manifest.json www/ 2>/dev/null || :

      - name: Build Android App
        run: |
          npx cap add android || npx cap sync android
          cd android
          ./gradlew assembleDebug --no-daemon

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: El-Sayeh-Store-APK
          path: android/app/build/outputs/apk/debug/app-debug.apk
